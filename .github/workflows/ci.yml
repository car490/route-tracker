name: CI

on:
  push:
    branches: ['**']
  pull_request:

jobs:
  pwa-test:
    name: Driver PWA — unit tests
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'
          cache-dependency-path: pcv-dashboard/busops/package-lock.json
      - run: npm ci
        working-directory: pcv-dashboard/busops
      - run: npm test
        working-directory: pcv-dashboard/busops

  dashboard-lint:
    name: Dashboard — lint
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'
          cache-dependency-path: pcv-dashboard/package-lock.json
      - run: npm ci
        working-directory: pcv-dashboard
      - run: npm run lint
        working-directory: pcv-dashboard

  dashboard-build:
    name: Dashboard — build
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'
          cache-dependency-path: pcv-dashboard/package-lock.json
      - run: npm ci
        working-directory: pcv-dashboard
      - run: npm run build
        working-directory: pcv-dashboard
